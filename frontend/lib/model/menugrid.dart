import 'package:flutter/material.dart';

class MenuItem {
  final String? title;
  final IconData? icon;
  final String? route;
  final Color? colors;

  MenuItem({this.title, this.icon, this.route, this.colors});
}
